/**
 * IFC Design System — barrel do pacote shared.
 *
 * Exporta apenas as utilidades JS (não-React). Para componentes React
 * reutilizáveis, use o barrel em `src/components/index.js`.
 */

export * from './options';
export * from './class-builder';
